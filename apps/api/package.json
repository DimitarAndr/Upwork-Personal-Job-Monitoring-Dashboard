{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node dist/main.js",
    "start:dev": "nest start --watch",
    "build": "nest build",
    "lint": "eslint \"src/**/*.ts\"",
    "format": "prettier --write \"src/**/*.ts\" \"prisma/**/*.prisma\"",
    "prisma:generate": "mkdir -p ./.prisma-engines-cache && PRISMA_ENGINES_CACHE_DIR=./.prisma-engines-cache prisma generate",
    "prisma:migrate": "mkdir -p ./.prisma-engines-cache && PRISMA_ENGINES_CACHE_DIR=./.prisma-engines-cache prisma migrate dev",
    "prisma:status": "mkdir -p ./.prisma-engines-cache && PRISMA_ENGINES_CACHE_DIR=./.prisma-engines-cache prisma migrate status"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "@prisma/client": "^6.0.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@nestjs/schematics": "^11.0.0",
    "@nestjs/testing": "^11.0.0",
    "@types/node": "^22.10.1",
    "prisma": "^6.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
